name: CI/CD Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  NODE_VERSION: '20.x'
  REGISTRY: ghcr.io
  REGISTRY_USERNAME: ${{ github.actor }}

jobs:
  # ==========================
  # Detect changes
  # ==========================
  detect-changes:
    name: Detect Changed Applications
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      landing: ${{ steps.filter.outputs.landing }}
      main-dashboard: ${{ steps.filter.outputs.main-dashboard }}
      chart: ${{ steps.filter.outputs.chart }}
      appgen: ${{ steps.filter.outputs.appgen }}
      packages: ${{ steps.filter.outputs.packages }}
      ideploy: ${{ steps.filter.outputs.ideploy }}
      ideploy-api: ${{ steps.filter.outputs.ideploy-api }}
      ideploy-web: ${{ steps.filter.outputs.ideploy-web }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Detect changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            api:
              - 'apps/api/**'
              - 'packages/**'
            landing:
              - 'apps/landing/**'
              - 'packages/**'
            main-dashboard:
              - 'apps/main-dashboard/**'
              - 'packages/**'
            chart:
              - 'apps/chart/**'
            appgen:
              - 'apps/appgen/**'
              - 'packages/**'
            packages:
              - 'packages/**'
            ideploy:
              - 'apps/ideploy/**'
            ideploy-api:
              - 'apps/ideploy-api/**'
              - 'packages/shared-models/**'
              - 'packages/shared-auth-client/**'
            ideploy-web:
              - 'apps/ideploy-web/**'
              - 'packages/shared-models/**'

  # ==========================
  # Quality checks
  # ==========================
  quality:
    name: Quality Checks
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.api == 'true' ||
      needs.detect-changes.outputs.landing == 'true' ||
      needs.detect-changes.outputs.main-dashboard == 'true' ||
      needs.detect-changes.outputs.chart == 'true' ||
      needs.detect-changes.outputs.appgen == 'true' ||
      needs.detect-changes.outputs.packages == 'true' ||
      needs.detect-changes.outputs.ideploy-api == 'true' ||
      needs.detect-changes.outputs.ideploy-web == 'true'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run Prettier format check
        run: npm run format:check
        continue-on-error: true

      - name: Run ESLint
        run: npm run lint:all
        continue-on-error: true

      - name: Build iDeploy API (typecheck)
        if: needs.detect-changes.outputs.ideploy-api == 'true'
        run: npm run build:ideploy-api

      - name: Typecheck iDeploy Web
        if: needs.detect-changes.outputs.ideploy-web == 'true'
        run: npm run typecheck:ideploy-web

  # ==========================
  # Summary
  # ==========================
  summary:
    name: CI/CD Summary
    runs-on: ubuntu-latest
    needs: [detect-changes, quality]
    if: always()
    steps:
      - name: Display CI/CD Summary
        run: |
          echo "## 📊 CI/CD Quality Checks Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Changes Detected:" >> $GITHUB_STEP_SUMMARY
          echo "- **API**: ${{ needs.detect-changes.outputs.api == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Landing Page**: ${{ needs.detect-changes.outputs.landing == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Main Dashboard**: ${{ needs.detect-changes.outputs.main-dashboard == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Chart**: ${{ needs.detect-changes.outputs.chart == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **AppGen**: ${{ needs.detect-changes.outputs.appgen == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Packages**: ${{ needs.detect-changes.outputs.packages == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **iDeploy API**: ${{ needs.detect-changes.outputs.ideploy-api == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **iDeploy Web**: ${{ needs.detect-changes.outputs.ideploy-web == 'true' && '✅ Changed' || '⏭️ No changes' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Quality Checks Status:" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ needs.quality.result == 'success' && '✅ Passed' || (needs.quality.result == 'skipped' && '⏭️ Skipped' || '❌ Failed') }}" >> $GITHUB_STEP_SUMMARY
