# Performance
prefer-offline=true
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Lockfile
package-lock=true

# Legacy peer deps (npm-compatible alternative to strict-peer-dependencies=false)
legacy-peer-deps=true

# Cache
# cache-min is deprecated, using prefer-offline instead
