/**
 * Le prompt d'édition de section est désormais GÉNÉRIQUE (partagé par les 3
 * documents). Conservé ici en ré-export pour compatibilité des imports existants.
 * @see ../../common/section-edit.prompt
 */
export {
  buildSectionEditPrompt,
  EDIT_FORMAT_RULES,
  EDITOR_COMPATIBILITY_RULES,
  type SectionEditPromptInput,
} from '../../common/section-edit.prompt';
